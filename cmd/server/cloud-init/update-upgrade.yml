#cloud-config

# Update package lists and upgrade all packages on first boot.

# Default login: ubuntu / ubuntu (dev convenience only — throwaway VMs).
chpasswd:
  expire: false
  users:
    - name: ubuntu
      password: ubuntu
      type: text
ssh_pwauth: true

package_update: true
package_upgrade: true
package_reboot_if_required: true
